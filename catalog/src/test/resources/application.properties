# Test configuration for catalogue service
quarkus.http.test-port=0

# Disable Consul registration in tests
consul.registration.enabled=false

# Disable OpenTelemetry for tests
quarkus.otel.enabled=false
