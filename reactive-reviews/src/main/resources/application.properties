# ===================================
# Reactive Reviews Service - Specific Configuration
# ===================================
# Shared properties are inherited from shared-config module
# Only service-specific overrides and unique properties are defined here

# HTTP Configuration - Service-specific port
quarkus.http.port=8084

# Reactive PostgreSQL client configuration
quarkus.datasource.reactive.url=postgresql://localhost:6544/reactive_reviews
quarkus.datasource.reactive.max-size=20

# Application name
quarkus.application.name=reactive-reviews-service

# ===================================
# OpenTelemetry - Distributed Tracing
# ===================================
# Service name appears in Zipkin traces
quarkus.otel.service.name=reactive-reviews-service
quarkus.otel.resource.attributes=service.namespace=library,service.version=1.0.0

# Hibernate Second-Level Cache - Entity-specific configuration
quarkus.hibernate-orm.cache."com.akfc.Review".expiration.max-idle=10M
quarkus.hibernate-orm.cache."com.akfc.Review".memory.object-count=1000
